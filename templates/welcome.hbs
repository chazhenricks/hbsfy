<h2>{{name}}!</h2>
<p>
    We bake the best most delecious cake varieties like
    {{#each cakeType}}
        {{cake}}{{topping}}
        {{#unless @last}}
        ,
        {{/unless}}
        {{#if @last}}
        .
        {{/if}}
    {{/each}}
</p>
